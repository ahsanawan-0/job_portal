@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --ngx-editor-icon-color: #767f8c;
  --ngx-editor-icon-hover-color: #4a5568;
  --ngx-editor-icon-active-color: #1a73e8;
}

.NgxEditor__MenuItem svg {
  fill: var(--ngx-editor-icon-color);
}

.NgxEditor__MenuItem:hover svg {
  fill: var(--ngx-editor-icon-hover-color);
}

.NgxEditor__MenuItem--Active svg {
  fill: var(--ngx-editor-icon-active-color);
}

.NgxEditor__MenuItem {
  cursor: pointer !important;
}

.NgxEditor__Content {
  min-height: 160px !important; 
  max-height: 400px; /* Optional: Set a maximum height for the editor */
  overflow-y: auto; /* Enable vertical scrolling if content exceeds max-height */
  
}

.NgxEditor__MenuBar {
  border: none;
}

.editor {
  border: 1px solid rgba(99, 98, 98, 0.2);
  border-radius: 8px;
  /* overflow: hidden;  */
  display: flex;
  flex-direction: column; 
}

.editor .NgxEditor__MenuBar {
  border-bottom-left-radius: 8px;
  border-bottom-right-radius: 8px;
  /* border-bottom: 1px solid rgba(0, 0, 0, 0.2); */
  /* background-color: #ff2f2f;  */
  z-index: 999999; /* Ensure the menu bar is on top */
}

.editor .NgxEditor {
  flex-grow: 1; 
  border: none; 
  border-top-left-radius: 8px;
  border-top-right-radius: 8px;
  /* border-radius: 8px; */
  background-color: aqua;

}

.editor .CodeMirror pre {
  white-space: pre !important; 
}

.editor .NgxEditor__MenuBar {
  padding: 10px;
}

/* Lists */
ul {
  list-style-type: disc;
  padding-left: 20px; 
}

ol {
  list-style-type: decimal; 
  padding-left: 20px; 
}

.NgxEditor__Content ul, 
.NgxEditor__Content ol {
  list-style-position: inside; 
  padding-left: 20px; /* Ensure proper padding for lists */
}

.NgxEditor__Content li {
  margin-bottom: 5px; 
}

/* Popup Styles */


/* Class to show the popup */
.NgxEditor__Popup--visible {
  display: block; /* Show the popup when this class is applied */
}
